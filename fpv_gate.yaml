# fpv_gate.yaml
# YOLOv8 data config for FPV gates

path: ./fpv_dataset      # Root folder of your generated dataset

train: images/train       # Training images
val: images/val           # Validation images

nc: 1                     # Number of classes (only gates)
names: ['gate']           # Class names
